<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.almurray.android.almurrayportal">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity"
            android:label="@string/title_activity_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".amigoPointForm" />
        <activity android:name=".amigoLoanForm" />
        <activity android:name=".positivePointForm" />
        <activity android:name=".manualReviewForm" />
        <activity android:name=".tipForm" />
        <activity android:name=".profileView" />
        <activity android:name=".feedBackForm" />
        <activity android:name=".AdminView" />
        <activity android:name=".listUsers" />
        <activity android:name=".georgeD" />
        <activity android:name=".TomK" />
        <activity android:name=".NickW" />
        <activity android:name=".editProfileView"></activity>
    </application>

</manifest>